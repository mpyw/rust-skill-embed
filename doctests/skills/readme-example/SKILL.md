---
name: readme-example
description: Gives the README's quick start something to embed. Nothing installs this skill.
---

# README example

`include_dir!("$CARGO_MANIFEST_DIR/skills")` needs a directory to read. This is
that directory, and nothing else reads it.
